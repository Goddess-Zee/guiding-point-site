---
import Layout from "../components/Layout.astro";
---
<Layout title="Contact | Guiding Point Consulting" path="/contact">
  <section class="py-14">
    <h1 class="text-4xl font-semibold">Contact</h1>
    <p class="mt-3 max-w-2xl text-white/70">
      Tell me what you’re trying to improve. I’ll reply with next steps and options.
    </p>

    <div class="mt-10 grid gap-6 sm:grid-cols-2">
      <form
        class="rounded-2xl border border-white/10 bg-white/5 p-6"
        method="POST"
        action="https://formspree.io/f/YOUR_FORM_ID"
      >
        <label class="block text-sm font-semibold">Name</label>
        <input name="name" required class="mt-2 w-full rounded-xl bg-slate-900/60 border border-white/10 px-4 py-3 outline-none focus:border-emerald-300" />

        <label class="mt-4 block text-sm font-semibold">Email</label>
        <input type="email" name="email" required class="mt-2 w-full rounded-xl bg-slate-900/60 border border-white/10 px-4 py-3 outline-none focus:border-emerald-300" />

        <label class="mt-4 block text-sm font-semibold">Message</label>
        <textarea name="message" required rows="5"
          class="mt-2 w-full rounded-xl bg-slate-900/60 border border-white/10 px-4 py-3 outline-none focus:border-emerald-300"></textarea>

        <button
          class="mt-5 w-full rounded-full bg-emerald-400 px-5 py-3 font-semibold text-slate-950 hover:bg-emerald-300 transition"
          type="submit"
        >
          Send
        </button>

        <p class="mt-3 text-xs text-white/50">
          Replace <code>YOUR_FORM_ID</code> after you create a Formspree form.
        </p>
      </form>

      <div class="rounded-2xl border border-white/10 bg-white/5 p-6">
        <h2 class="font-semibold">Prefer a quick call?</h2>
        <p class="mt-2 text-sm text-white/70">
          Add your Calendly link here (or any booking tool).
        </p>
        <a class="mt-4 inline-block text-emerald-300 hover:text-emerald-200 font-semibold"
           href="https://calendly.com/YOUR-LINK" target="_blank" rel="noreferrer">
          Book time →
        </a>

        <div class="mt-8">
          <h3 class="font-semibold">Email</h3>
          <p class="mt-2 text-sm text-white/70">you@yourdomain.com</p>
        </div>
      </div>
    </div>
  </section>
</Layout>
