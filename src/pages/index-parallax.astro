---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="Guiding Point Consulting - Web + AI Consulting"
  description="Web + AI consulting that bridges tech, creativity, and human connection."
>
  <!-- Hero Section with Parallax -->
  <section class="hero parallax-section">
    <!-- Parallax Background Layers -->
    <div class="parallax-bg" data-speed="0.5">
    <div class="gradient-orb orb-1"></div>
    <div class="gradient-orb orb-2"></div>
  </div>
    
    <div class="container hero-content">
      <p class="eyebrow">Human-centered web + AI content systems</p>
      <h1>Modernize your website and content ops—without losing the human touch.</h1>
      <p class="hero-text">
        I modernize websites and build AI-enabled content operations so teams publish faster, stay consistent,
        and improve measurable outcomes—while serving as the liaison between leadership and technical teams.
      </p>
      <div class="cta-group">
        <a href="/contact" class="btn btn-primary">Request a Website Audit</a>
        <a href="/case-studies" class="btn btn-secondary">View Case Studies</a>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="services">
    <div class="container">
      <div class="services-grid">
        <div class="service-card">
          <div class="service-number">01</div>
          <h3>Website Audit + Roadmap</h3>
          <p>Clarity on what to fix first and why.</p>
          <ul>
            <li>UX + content structure review</li>
            <li>Performance + accessibility quick wins</li>
            <li>Prioritized roadmap with impact notes</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-number">02</div>
          <h3>AI Content Ops Setup</h3>
          <p>SOPs, prompts, QA, and workflows that scale.</p>
          <ul>
            <li>Content workflow mapping</li>
            <li>Prompt library tailored to your brand</li>
            <li>QA checklist and templates</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-number">03</div>
          <h3>Fractional Strategy Lead</h3>
          <p>A steady hand bridging leadership + delivery.</p>
          <ul>
            <li>Weekly/biweekly strategy alignment</li>
            <li>Stakeholder coordination</li>
            <li>Performance tracking and iteration</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="stats">
    <div class="container">
      <div class="stats-grid">
        <div class="stat">
          <div class="stat-number">+28%</div>
          <div class="stat-label">ROAS lift</div>
        </div>
        <div class="stat">
          <div class="stat-number">+42%</div>
          <div class="stat-label">Traffic growth</div>
        </div>
        <div class="stat">
          <div class="stat-number">-15%</div>
          <div class="stat-label">Time-to-publish</div>
        </div>
      </div>
    </div>
  </section>

  <!-- How I Work Section -->
  <section class="how-i-work">
    <div class="container">
      <h2>How I work</h2>
      <p class="section-intro">Strategy first. Then clean execution. Then systems that help your team keep moving after launch.</p>
      
      <div class="process-grid">
        <div class="process-step">
          <div class="step-number">1</div>
          <h4>Diagnose</h4>
          <p>Review your site and content workflow to identify quick wins and gaps.</p>
        </div>
        <div class="process-step">
          <div class="step-number">2</div>
          <h4>Build</h4>
          <p>Design systems, SOPs, and templates that scale with your team.</p>
        </div>
        <div class="process-step">
          <div class="step-number">3</div>
          <h4>Lead</h4>
          <p>Connect the dots between leadership goals and delivery teams.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Ready to modernize?</h2>
        <p>I'll review your site and content workflow, identify quick wins, and map a practical modernization plan.</p>
        <a href="/contact" class="btn btn-primary">Request an Audit</a>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Parallax Hero Styles */
  .parallax-section {
    position: relative;
    overflow: hidden;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .parallax-bg {
    position: absolute;
    top: -50%;
    left: 0;
    right: 0;
    bottom: -50%;
    z-index: 0;
    will-change: transform;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.4;
    animation: float 20s ease-in-out infinite;
  }

  .orb-1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, var(--rust) 0%, transparent 70%);
    top: -10%;
    left: -10%;
    animation-delay: 0s;
  }

  .orb-2 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, var(--beige) 0%, transparent 70%);
    bottom: -10%;
    right: -10%;
    animation-delay: -10s;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    25% {
      transform: translate(30px, -30px) scale(1.05);
    }
    50% {
      transform: translate(-20px, 20px) scale(0.95);
    }
    75% {
      transform: translate(20px, 30px) scale(1.02);
    }
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  /* Rest of the styles remain the same */
  .hero {
    padding: var(--space-3xl) 0;
    text-align: center;
  }

  .eyebrow {
    display: inline-block;
    color: var(--rust);
    font-weight: 600;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-md);
    padding: 0.5rem 1rem;
    background: rgba(172, 52, 48, 0.08);
    border-radius: 20px;
  }

  .hero h1 {
    max-width: 900px;
    margin: 0 auto var(--space-lg);
  }

  .hero-text {
    max-width: 700px;
    margin: 0 auto var(--space-xl);
    font-size: 1.25rem;
    color: var(--gray-600);
  }

  .cta-group {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .gradient-orb {
      width: 300px;
      height: 300px;
    }

    .parallax-section {
      min-height: auto;
      padding: var(--space-2xl) 0;
    }
  }
</style>

<script>
  // Simple parallax effect
  window.addEventListener('scroll', () => {
    const scrolled = window.pageYOffset;
    const parallaxBg = document.querySelector('.parallax-bg');
    
    if (parallaxBg && scrolled < window.innerHeight) {
      parallaxBg.style.transform = `translateY(${scrolled * 0.5}px)`;
    }
  });

  // Optional: Add smooth scroll
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
</script>